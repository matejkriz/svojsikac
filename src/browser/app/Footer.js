// @flow
import React from 'react';

const styles = {
  footer: {
    backgroundColor: '#000',
    color: '#fff',
    display: 'flex',
    padding: '1em',
  },
};

const Footer = () => (
  <div style={styles.footer}>
    Footer
  </div>
);

export default Footer;
